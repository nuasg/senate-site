%h1 Affiliations
%div
  .item-listing.add
    = link_to(controller: :affiliations, action: :new) do
      %span.fas.fa-plus-circle
      &nbsp;Add an Affiliation
.table-responsive
  %table.table
    %tr
      %th Affiliation Name
      %th Type
      %th Representative
      %th Enabled
      %th Actions
    - @affiliations.each do |a|
      %tr
        %td= a.name
        %td= a.type.name
        %td= a.user.nil? ? content_tag(:span, 'None', class: 'text-muted') : a.user.name
        %td= a.enabled ? 'Yes' : 'No'
        %td
          = link_to(edit_icon, action: :edit, id: a.id)
          = link_to delete_icon, {action: :destroy, id: a.id}, method: :delete, data: {confirm: 'Are you sure you want to delete this?'}